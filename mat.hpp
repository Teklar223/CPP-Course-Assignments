#include <string>
#include <iostream>
using namespace std;

namespace ariel{
    string mat(int col, int row, char sym1, char sym2);
    string mat_alt(int col, int row, char sym1, char sym2); //allows even rows or columns! - for Main.cpp showoff
}